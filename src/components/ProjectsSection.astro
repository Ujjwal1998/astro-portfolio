---
import ProjectCard from "./ProjectCard.astro";
import { info } from "../data/info";
---

<section class="my-16 grid grid-cols-1 gap-4 md:grid-cols-2">
  {info.projects.map((project) => <ProjectCard projectDetail={project} />)}
</section>
